C#

Array kernel = new object[] {
    1, 1, 1,
    1, 1, 1,
    1, 1, 1
};

convolutionFilter.Kernel = kernel;

vbNet

Dim kernel As Array = New Object() {1, 1, 1, 1, 1, 1, _
	1, 1, 1}

convolutionFilter.Kernel = kernel
